language: python
python:
    - "2.6"
    - "2.7"
    - "3.3"
    - "3.4"
    - "3.5"

install:
    - python setup.py install
    - pip install pytest pytest-cov coveralls

script:
    - py.test test.py -v --cov trans --cov-report term-missing

after_success:
    - coveralls
